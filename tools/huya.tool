allow pasting

streamInfo = window.hyPlayerConfig.stream.data[0].gameStreamInfoList[0];

url = streamInfo.sFlvUrl + '/' + streamInfo.sStreamName + '.' + streamInfo.sFlvUrlSuffix + '?' + streamInfo.sFlvAntiCode;